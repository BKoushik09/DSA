#finding the occurences of each element
'''arr = list(map(int, input("enter numbers:").split()))
freq = {}
for num in arr:
    if num in freq:
        freq[num] += 1
    else:
        freq[num] = 1
for key in freq:
    print(key, ':', freq[key], 'times')'''